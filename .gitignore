# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sw?

# Env files
frontend/.env
frontend/frontend/.env

# Local implementation notes (do not commit)
AWS_SAM_ORS_IMPLEMENTATION.local.md

# ── AWS SAM ───────────────────────────────────────────────────────────────────
backend/.aws-sam/
backend/samconfig.toml

# ── .NET / C# ─────────────────────────────────────────────────────────────────
# Build output
backend-dotnet/**/bin/
backend-dotnet/**/obj/

# Visual Studio artifacts
.vs/
*.user

# Azure Functions — local secrets (NEVER commit, contains ORS_API_KEY)
backend-dotnet/**/local.settings.json

# Azure Functions worker extensions (auto-generated during build)
backend-dotnet/**/WorkerExtensions/

# NuGet packages (restored automatically, no need to commit)
*.nupkg

# ── Azurite local emulator storage ────────────────────────────────────────────
__blobstorage__/
__queuestorage__/
__azurite_db_*.json
AzuriteConfig
