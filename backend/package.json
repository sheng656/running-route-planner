{
  "name": "running-route-planner-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "sam build --template-file template.yaml",
    "deploy:guided": "sam deploy --guided --template-file template.yaml",
    "deploy": "sam deploy --template-file template.yaml",
    "local:generate": "sam local invoke GenerateRouteFunction --event events/generate-route.json --template-file template.yaml",
    "local:gpx": "sam local invoke ExportGpxFunction --event events/export-gpx.json --template-file template.yaml"
  },
  "devDependencies": {
    "@aws-sdk/client-secrets-manager": "^3.936.0",
    "@types/aws-lambda": "^8.10.156",
    "@types/node": "^24.12.2",
    "typescript": "^5.9.3"
  }
}
